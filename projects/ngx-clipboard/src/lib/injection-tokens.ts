import { InjectionToken } from '@angular/core';

import { ClipboardParams } from './interface';

export const CLIPBOARD_ROOT_OPTIONS = new InjectionToken<ClipboardParams>('CLIPBOARD_ROOT_OPTIONS');
